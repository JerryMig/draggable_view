<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/main_container"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@mipmap/background"
    tools:context=".controller.MainActivity">

    <project.jerry.panobike.view.DraggableView
        android:id="@+id/draggable_image"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content" />

    <project.jerry.panobike.view.InformationView
        android:id="@+id/information_view"
        android:layout_width="match_parent"
        android:layout_height="wrap_content" />

    <project.jerry.panobike.view.ClickEffectButton
        android:id="@+id/fire_btn"
        android:layout_width="100dp"
        android:layout_height="100dp"
        android:layout_alignParentBottom="true"
        android:layout_centerHorizontal="true"
        android:layout_marginBottom="12dp"
        android:onClick="fireMissile"
        android:src="@mipmap/fire" />

</RelativeLayout>